import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Modify Habits',
  };

export default function Page() {
    return <p>This is where the tracked habits can be changed.</p>;
}
